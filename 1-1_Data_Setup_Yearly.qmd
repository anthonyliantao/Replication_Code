---
title: "处理年份数据"
format:
  html:
    toc: true
    toc-location: left
    toc-depth: 3
    toc-title: "目录"
execute:
  echo: true
  warning: false
  message: true
---

## 0. 简介

本笔记基于 [`1-1_Data_Setup_Yearly.R`](1-1_Data_Setup_Yearly.R) 脚本，详解其数据处理逻辑: 

原脚本的处理逻辑：

- 读取原始 CPS-ASEC 职业流动数据；
- 合并细分类职业标签（如教师与助教）；
- 排除 NEC（未另分类）类别；
- 生成按年分组的职业流动数据与权重。

本笔记的修改：

- 生成 fake data 以支持继续操作
- 将部分重要操作的 data.frame 语法改为了熟悉的 tidyverse 语法
- 增加了文字解释

## 1. 环境设置

```{r}
library(here)
library(dplyr)
library(tidyverse)
library(readr)
library(data.table)
library(DT)
```

## 2. 读取数据

本脚本依赖根据完整的CPS-ASEC 职业流动数据，以及男女分样本。虽然原始数据暂时不可得，但根据 [README](README.md) 中的描述，我们可以推断出数据文件 `edges_67to15ASEC_dropimp_primeage.dta` 的结构。

### 2.1. 数据简介

- 数据来源：CPS-ASEC 1975–2014 样本
- 样本范围：25–55 岁就业者
- 过滤条件：排除了职业信息为“插补值”（imputed occupation）的个体
- 单位：每条记录是一笔职业转移（从 occ1 → occ2）

### 2.2. 字段结构

| 变量名 | 类型 | 含义        |
|-----|-----|---------------|
| `occ1`        | character  | 转出职业（起点）职业名称|
| `occ2`        | character  | 转入职业（终点）职业名称|
| `year_begin`  | integer    | 转移的起始年份（通常是受访年份 - 1）|

### 2.3. 生成假数据集

```{r make_data}
# 设置随机种子，保证可复现
set.seed(2025)

# 读取美国普查1990职业列表
# https://usa.ipums.org/usa/volii/occ1990.shtml
occ1990 <- read_csv(
  here("helper", "occ_dictionary.csv"),
  show_col_types = FALSE,
  col_types = cols(.default = col_character())
)
occ_list <-  occ1990$occ_label

# 随机生成职业转移记录
n <- 20000
fake_data <- tibble(
  occ1 = sample(occ_list, n, replace = TRUE),
  occ2 = sample(occ_list, n, replace = TRUE),
  year_begin = sample(1975:2013, n, replace = TRUE)
)

# 分别采样男女子集
fake_data_m <- fake_data %>% slice_sample(n = n / 2)
fake_data_f <- fake_data %>% slice_sample(n = n / 2)

# 保存到文件（覆盖写入）
haven::write_dta(fake_data, here("rawdata", "edges_67to15ASEC_dropimp_primeage.dta"))
haven::write_dta(fake_data_m, here("rawdata", "edges_67to15ASEC_dropimp_primeage_m.dta"))
haven::write_dta(fake_data_f, here("rawdata", "edges_67to15ASEC_dropimp_primeage_f.dta"))
```

### 2.4. 读取数据集

```{r load_data}
# 分析起始年份
start_year = 1989

# 样本设置
# 注意: “FullSamp”（总体样本）必须放在最后一项
# 具体原因见 Gen_Period_Data.R 脚本中的说明
samp_spec = c(
  "Male",    # 男性样本
  "Female",  # 女性样本
  "FullSamp" # 总体样本
)

# 确保 FullSamp 在样本规格中的顺序最后
if (samp_spec[length(samp_spec)] != "FullSamp") {
  samp_spec <- c(setdiff(samp_spec, "FullSamp"), "FullSamp")
}

for (s_spec in samp_spec) {
    
    d_file <- switch(s_spec,
        "FullSamp" = "edges_67to15ASEC_dropimp_primeage.dta",
        "Male"     = "edges_67to15ASEC_dropimp_primeage_m.dta",
        "Female"   = "edges_67to15ASEC_dropimp_primeage_f.dta",
        stop("Unknwon sample specification!")
    )

    dat <- readstata13::read.dta13(here::here("rawdata", d_file)) %>%
        data.table() %>%
        mutate(int_year = year_begin + 1L) %>%
        select(occ1, occ2, int_year) %>%
        filter(int_year >= start_year)
}

datatable(
  head(dat),
  rownames = FALSE,
  options = list(
    dom = 't'
  )
)
```


## 3. 数据处理

- 将类似 "Subject instructors" 合并为统一类别；
- 将 "Teacher's aides" 合并到 "Teachers , n.e.c."；

```{r}
# 在起始与终点职业中查找包含 "instruct" 的职业名称
ins_1 <- grep("instruct", unique(dat$occ1), value = TRUE, ignore.case = TRUE)
ins_2 <- grep("instruct", unique(dat$occ2), value = TRUE, ignore.case = TRUE)
# 合并起点和终点职业中所有含 instruct 的名称并去重
instructors <- unique(c(ins_1, ins_2))
# 移除不想合并的两个特定职业，保留其他“instructor”职业
instructors <- setdiff(
    instructors, 
    c("Athletes, sports instructors, and officials", "Subject instructors (HS/college)")
)
# 将 属于 instructors 的职业统一替换为 "Subject instructors (HS/college)"
dat[occ1 %in% instructors, occ1 := "Subject instructors (HS/college)"]
dat[occ2 %in% instructors, occ2 := "Subject instructors (HS/college)"]

# 将名为"Teacher's aides" 的职业合并到 "Teachers , n.e.c."
dat[occ1 == "Teacher's aides", occ1 := "Teachers , n.e.c."]
dat[occ2 == "Teacher's aides", occ2 := "Teachers , n.e.c."]
```

## 4. 保存清洗数据

```{r}
# 保存完整版本
if (s_spec == "FullSamp") {
    fwrite(dat[, .(occ1, occ2, int_year)],
    here("data", paste0("dat_", s_spec, "_FullOccs.csv")))
}

fwrite(
    # dat[, .N, by = .(occ1, occ2, int_year)],
    dat %>%
        group_by(occ1, occ2, int_year) %>%
        summarise(N = n(), .groups = "drop"),
    here("data", paste0("dat_", s_spec, "_agg_FullOccs.csv"))
    )

# NEC 类别的正则表达式，用于识别未具体分类职业
nec_regex = c(
  "n\\.e\\.c", 
  "n\\. e\\. c\\.",
  "nec", 
  "elsewhere"
) 

# 排除 NEC 类别
nec_pattern <- paste0(nec_regex, collapse = "|")
nonec_dat <- dat[!grepl(nec_pattern, occ1, ignore.case = TRUE) & !grepl(nec_pattern, occ2, ignore.case = TRUE)]

if (s_spec == "FullSamp") {
    fwrite(nonec_dat[, .(occ1, occ2, int_year)], 
    here::here("data", paste0("dat_", s_spec, "_NoNEC.csv")))
}

fwrite(
    #nonec_dat[, .N, by = .(occ1, occ2, int_year)], 
    nonec_dat %>%
        group_by(occ1, occ2, int_year) %>%
        summarise(N = n(), .groups = "drop"),
    here::here("data", paste0("dat_", s_spec, "_agg_NoNEC.csv"))
)
```

将清洗后的数据保存为四个主要文件：

1. **`dat_FullSamp_FullOccs.csv`**  
   - 包含全样本（FullSamp）中所有职业类别的原始转移数据（未聚合）。  
   - 每一行为一条职业转移记录，包含起始职业（`occ1`）、目标职业（`occ2`）和对应年份（`int_year`）。  

2. **`dat_FullSamp_agg_FullOccs.csv`**  
   - 对全样本职业转移数据按职业对和年份聚合后的数据。  
   - 每条记录为某起始职业转移到某目标职业在某年份发生的次数（用 `.N` 计数）。  

3. **`dat_FullSamp_NoNEC.csv`**  
   - 从全样本中排除了 NEC（未具体分类职业）类别后，保留的原始转移数据（未聚合）。  
   - 便于排除不明确职业对后，进行更精确的职业流动分析。  

4. **`dat_FullSamp_agg_NoNEC.csv`**  
   - 排除 NEC 后的职业转移数据聚合版本，统计每对职业在各年份的转移次数。  

## 5. 生成标准化年份权重

```{r}
# 权重标准化的基准年份
w_stand_year = 1989

# 含 NEC 的权重
w_year <- dat %>%
  count(int_year) %>%
  mutate(
    n_stand = n[int_year == w_stand_year],
    obs_weights = n_stand / n
  ) %>%
  select(-n)  # 去掉样本计数列

fwrite(w_year, here("data", paste0("obs_weights_", s_spec, "_FullOccs.csv")))

# 不含 NEC 的权重
w_year_nonec <- nonec_dat %>%
  count(int_year) %>%
  mutate(
    n_stand = n[int_year == w_stand_year],
    obs_weights = n_stand / n
  ) %>%
  select(-n)

fwrite(w_year_nonec, here("data", paste0("obs_weights_", s_spec, "_NoNEC.csv")))
```

生成权重，对不同年份的样本规模进行标准化，确保基准年份（1989）的观察值数为参考值，其他年份根据样本大小进行调整。

- **含 NEC 权重**：基于全部职业类别的样本；
- **不含 NEC 权重**：剔除未具体分类职业后的样本。

## 6. 筛选出稳定职业

自定义函数：`get_missing_pattern()`
```{r}
#| code-fold: true
#| code-summary: "查看完整代码"
#' 获取每个职业在不同时间窗口中的缺失模式
#' 
#' @param dat 一个 data.table 数据对象，必须包含列 occ1, occ2, int_year（职业转移数据）
#' @param w_size 滑动窗口大小（可取 1, 3, 5）
#' @param include_isolates 是否保留自环或孤立职业（默认 FALSE，即去除 occ1 == occ2 的转移）
#' @return 返回一个 data.table，列出每个职业在每个年份的出现情况以及缺失模式
get_missing_pattern = function(
    dat, 
    w_size = c(1, 3, 5),
    include_isolates = FALSE) 
{
    # 检查输入数据是否包含必需的列
    if (!all(c("occ1", "occ2", "int_year") %in% names(dat)))
        stop("dat has to contain the columns occ1, occ2, and int_year")
    
    # 检查 w_size 是否有效
    if (!all(w_size %in% c(1, 3, 5)))
        stop("w_size has to be a subset of c(1, 3, 5)")
    
    # 如果不保留孤立职业（自环），则去除 occ1 == occ2 的记录
    if (!include_isolates) 
        dat = dat[occ1 != occ2]
    
    # 获取所有出现过的职业（无重复）
    occs  = dat[, unique(c(occ1, occ2))]

    # 获取所有年份并转为字符向量（便于作为列名）
    y_char = paste0(dat[, sort(unique(int_year))])
    
    # 统计每个职业在哪些年份中出现过（按 int_year 分组后展开）
    m_dat = dat[
        , .(occ_label = unique(c(occ1, occ2))), by = int_year
    ] %>%
        dcast(occ_label ~ int_year, value.var = "int_year")
    
    # 将出现记录转为 1（出现）/0（缺失）
    m_dat[
        , 
        (y_char) := lapply(.SD, function(w) as.numeric(!is.na(w))), 
        .SDcols = y_char
    ][
        ,
        # 计算每个职业缺失的年份数量
        n_miss := length(y_char) - rowSums(.SD), 
        .SDcols = y_char
    ]
    
    # 将每个职业的年份出现情况合并为一个字符串（用于匹配缺失模式）
    m_dat[
        , m_pattern := apply(m_dat[, y_char, with = F],
                             1L, 
                             paste0, 
                             collapse = "")
    ]
    
    # 遍历每个窗口长度（w_size），统计在该窗口大小下缺失的连续“0”片段数
    
    for (w in w_size) {
        
        m_dat[, `:=`
            (
                paste0("m_count_win_", w),
                stringr::str_count(
                    m_pattern, 
                    # 匹配连续 w 个 0 的字符串
                    paste0(rep("0", w), collapse="")
                )
            )
        ]
        
    }
    
    # 返回结果表格（每行一个职业，附带各窗口下的缺失统计
    return(m_dat)
    
}
```

```{r}
# 读取不含 NEC 的聚合转移数据
nonec_dat = fread(here("data", "dat_FullSamp_agg_NoNEC.csv"))

# 获取职业在各个 5 年滑动窗口中是否缺失的模式
m_pat = get_missing_pattern(
    dat = nonec_dat, 
    w_size = 5,
    include_isolates = FALSE)

datatable(
  head(m_pat),
  options = list(dom = 't')
)
```

---

🔹 `m_pattern`：年份出现模式字符串

- 某个职业在各年份中是否出现，1（出现）和 0（缺失）组成的二进制字符串；

🔹 m_count_win_{w}：连续缺失窗口计数

- 统计该职业在整个时间序列中，**出现了多少次“连续5年都未出现”**的情况；
- 对于 `m_pattern` 10000011000001，值为2

---

```{r}
# 选出在所有 5 年窗口中都未缺失的职业
occ_5 = m_pat[m_count_win_5 == 0]$occ_label

# 从原始数据中过滤只保留这些完整职业的转移数据
nonec_dat_5 = nonec_dat[(occ1 %in% occ_5) & (occ2 %in% occ_5)]

# 检查职业子集是否一致
if (length(occ_5) != length(nonec_dat_5[, unique(c(occ1, occ2))]))
    stop("dimension mismatch after subsetting")
  
# 保存数据
fwrite(nonec_dat_5, here("data", "dat_FullSamp_agg_NoNEC_complete5.csv"))
```

## 7. 建立职业名称映射表

```{r}
dict = read_csv(here("helper", "occ_dictionary.csv")) %>%
    data.table

# 确保职业名称是字符型，避免后续合并出错
dict = dict[, occ_label := as.character(occ_label)]

# 从分析数据中提取实际出现的职业名称（occ1/occ2）
# 赋予一个自增的职业 ID
nonec_dat[
    , .(occ_label = unique(c(occ1, occ2)))
][
    , occ_id := .I
]

# 将官方字典（含 OCC1990 编码）与实际出现职业名称合并
dict = dict %>%
    merge(
        nonec_dat[
            , .(occ_label = unique(c(occ1, occ2)))
        ][
            , occ_id := .I
        ],
        by = "occ_label",
        all.y = TRUE
    ) %>%
    setnames("occ_numeric", "occ_1990")

# 检查完整性
# 是否有ID缺口
if (max(dict$occ_id) != nrow(dict))
    stop("some occ_ids are missing")
# 是否所有出现的职业都能匹配到字典
if (nrow(dict) != length(nonec_dat[, unique(c(occ1, occ2))]))
    stop("some occupations in the dataset are not in the dictionary")

# 保存字典
fwrite(dict, here("data", "labmap_NoNEC.csv"))

datatable(
  dict
)
```
